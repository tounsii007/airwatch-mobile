<?xml version="1.0" encoding="utf-8"?>
<!--
  Rounded-rect background for the home widget. Light fill so the
  numbers stay readable against any wallpaper; corner radius matches
  the app's `panelBorderRadius` token.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#F5F8FA" />
    <corners android:radius="20dp" />
    <stroke android:width="0.5dp" android:color="#E2E8F0" />
</shape>
